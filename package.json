{
	"name": "frontend-microservices-monorepo",
	"version": "1.0.0",
	"private": true,
	"workspaces": [
		"packages/*",
		"services/*"
	],
	"scripts": {
		"lint": "npx eslint",
		"format": "prettier --write \"**/*.{ts,tsx,md}\"",
		"preview": "concurrently \"cd services/about && npm run dev\" \"cd services/shop && npm run dev\" \"cd services/host && npm run dev\""
	},
	"devDependencies": {
		"prettier": "latest"
	}
}

